// export const API_URL = process.env.API_URL || "https://tasks.advartit.in"; 

export const API_URL = process.env.API_URL || "https://tasks.advartit.in"; // Fallback to localhost if not defined


// export const API_URL = process.env.API_URL || "http://localhost:8000"; // Fallback to localhost if not defined


