// export const API_URL = process.env.API_URL || "https://data.advartit.in";

export const API_URL = process.env.API_URL || "http://localhost:9000" || "https://data.advartit.in"; // Fallback to localhost if not defined
